name: Android NDK Build (PanVK Native)

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-android-wrapper:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        build-type: [release]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Patch anon_file.c for memfd_create
        run: |
          sed -i 's/#if defined(HAVE_MEMFD_CREATE) \&\& !defined __TERMUX__/#if defined(HAVE_MEMFD_CREATE)/' src/util/anon_file.c

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
              build-essential \
              libelf-dev \
              spirv-tools \
              bison \
              flex \
              libdrm-dev \
              pkg-config \
              gettext \
              llvm-18-dev \
              libclang-18-dev \
              libclc-18-dev
          python -m pip install --upgrade pip
          pip install mako PyYAML meson ninja packaging

          # CREACIÓN DE SHIMS LOCALES PARA EL ENTORNO DEL HOST
          mkdir -p ${{ github.workspace }}/host-shims

          # Generación del Shim libclc local
          cat << 'EOF' > ${{ github.workspace }}/host-shims/libclc.pc
          Name: libclc
          Description: Standalone libclc stub for Mesa host tools compilation
          Version: 1.0.0
          Cflags: -I/usr/include
          Libs: 
          EOF

          # 🟢 CORRECCIÓN CENTRAL: Forzamos la versión 18.1.0 exigida rígidamente por Meson
          cat << 'EOF' > ${{ github.workspace }}/host-shims/LLVMSPIRVLib.pc
          Name: LLVMSPIRVLib
          Description: Standalone LLVMSPIRVLib stub for Mesa host tools compilation
          Version: 18.1.0
          Cflags: -I/usr/include
          Libs: 
          EOF

      # GENERACIÓN DE HERRAMIENTAS DE HOST
      - name: Generate Host Tools
        run: |
          export PKG_CONFIG_PATH=${{ github.workspace }}/host-shims:$PKG_CONFIG_PATH
          meson setup build-host \
              -Dplatforms=[] \
              -Dgallium-drivers=[] \
              -Dvulkan-drivers=[] \
              -Dtools=panfrost \
              -Dprecomp-compiler=enabled \
              -Dinstall-precomp-compiler=true \
              -Dllvm=enabled \
              -Dmesa-clc=enabled -Dinstall-mesa-clc=true

          ninja -C build-host src/compiler/clc/mesa_cl_dummy 2>/dev/null || true
          ninja -C build-host src/compiler/clc/mesa_clc src/compiler/spirv/vtn_bindgen2

          ln -sf "$GITHUB_WORKSPACE/build-host/src/compiler/clc/mesa_cl_dummy" "$GITHUB_WORKSPACE/build-host/src/compiler/clc/mesa-cl-dummy" 2>/dev/null || true
          ln -sf "$GITHUB_WORKSPACE/build-host/src/compiler/clc/mesa_clc" "$GITHUB_WORKSPACE/build-host/src/compiler/clc/mesa-clc"
          ln -sf "$GITHUB_WORKSPACE/build-host/src/compiler/spirv/vtn_bindgen2" "$GITHUB_WORKSPACE/build-host/src/compiler/spirv/vtn-bindgen2"
          echo "$GITHUB_WORKSPACE/build-host/src/compiler/clc" >> $GITHUB_PATH
          echo "$GITHUB_WORKSPACE/build-host/src/compiler/spirv" >> $GITHUB_PATH

      # CONFIGURACIÓN DEL ENTORNO DE COMPILACIÓN CRUZADA (ANDROID AARCH64)
      - name: Configure Cross-File Matrix
        run: |
          cat << 'EOF' > android-cross.txt
          [binaries]
          c = '$ANDROID_NDK_LATEST_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android30-clang'
          cpp = '$ANDROID_NDK_LATEST_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android30-clang++'
          ar = '$ANDROID_NDK_LATEST_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-ar'
          strip = '$ANDROID_NDK_LATEST_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-strip'
          pkg-config = '/usr/bin/pkg-config'

          [properties]
          needs_exe_wrapper = true

          [host_machine]
          system = 'android'
          cpu_family = 'aarch64'
          cpu = 'aarch64'
          endian = 'little'
          EOF
          envsubst < android-cross.txt > android-cross-compiled.txt

      # FASE CENTRAL DE CONFIGURACIÓN DEL DRIVER VULKAN PANVK PURIFICADO
      - name: Configure Meson
        run: |
          meson setup build \
              --cross-file android-cross-compiled.txt \
              -Dbuildtype=${{ matrix.build-type }} \
              -Dplatforms=android \
              -Dandroid-stub=true \
              -Dandroid-libbacktrace=disabled \
              -Dplatform-sdk-version=30 \
              -Dglx=disabled \
              -Dgbm=disabled \
              -Degl=disabled \
              -Dopengl=false \
              -Dgles1=disabled \
              -Dgles2=disabled \
              -Dglvnd=disabled \
              -Dvalgrind=disabled \
              -Dgallium-drivers= \
              -Dshared-glapi=disabled \
              -Dzstd=disabled \
              -Dmesa-clc=system \
              -Dprecomp-compiler=system \
              -Dvulkan-drivers=panfrost \
              -Dllvm=disabled \
              -Dpanfrost-kmds=kbase,panthor

      # COMPILACIÓN DEL BINARIO GRÁFICO FINAL
      - name: Compile Project
        run: |
          meson compile -C build

      - name: Grant Execute Permissions to Package Script
        run: chmod +x ci-scripts/*.sh

      # RESCATE Y MUTACIÓN EXTERNA COMPATIBLE CON WINLATOR
      - name: Structure and Pack Final Container Package
        run: ./ci-scripts/package_wrapper.sh

      - name: Upload Final wrapper.tzst
        uses: actions/upload-artifact@v4
        with:
          name: Mesa-Wrapper-Zstd
          path: wrapper.tzst
          if-no-files-found: error
